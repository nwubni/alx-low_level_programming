#include <stdio.h>
/**
 * main - assigns a random number to int n everytime
 * prints all English alphabets
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char ch = 'a'; ch <= 'z'; ch++)
        	putchar(ch);
        
	printf("\n");
	
	return (0);
}
